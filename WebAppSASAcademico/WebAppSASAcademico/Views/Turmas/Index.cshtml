@model IEnumerable<WebAppSASAcademico.Models.Turma>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.condicaoLimiteUm)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.condicaoLimiteDois)
        </th>*@
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.condicaoLimiteUm)
        </td>
        @*<td>
            @Html.DisplayFor(modelItem => item.condicaoLimiteDois)
        </td>*@
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTurma }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTurma }) |
          
        </td>
    </tr>
}

</table>
